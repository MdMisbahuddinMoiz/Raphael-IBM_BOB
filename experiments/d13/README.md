# D13 capability-fabric experiments (historical notes only)

> The D13 modules were promoted into `raphael_ibm_bob/` as the D13
> implementation (`capability_registry.py`, `capability_selector.py`,
> `capability_prerequisites.py`, `observation_model.py`,
> `target_service_model.py`, `credential_provenance.py`,
> `capability_bootstrap.py`). This dir is retained only as
> historical and experimental notes. Don't import from here. The governed
> implementation lives in `raphael_ibm_bob/`, and its status is
> implementation in review pending integration validation. See
> `docs/D13_CAPABILITY_FABRIC.md`.
