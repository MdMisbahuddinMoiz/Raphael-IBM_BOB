"""raphael_ibm_bob.http.views — read-only HTML view builders.

These modules render operator screens from the SAME authoritative data
the JSON API exposes (`harness.api`). They are presentation only: no
execution authority, no second store, no new network path.
"""
